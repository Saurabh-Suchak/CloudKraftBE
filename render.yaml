services:
  - type: web
    name: cloudkraftbe
    env: python
    buildCommand: pip install -r requirements.txt && bash scripts/install_terraform.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
